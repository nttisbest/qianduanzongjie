# git

```git
git remote -v 查看远程状态

git remote add upstream 地址   确定一个将被同步给 fork 远程的上游仓库 


git fetch upstream  从上游仓库 fetch 分支和提交点，提交给本地 master，并会被存储在一个本地分支 upstream/master 


git merge upstream/master

git push origin master
```

